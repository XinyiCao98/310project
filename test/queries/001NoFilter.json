{
    "title": "SELECT dept, id, avg WHERE avg == 95 && dept == 'adhe' ORDER BY avg",
    "query": {
        "WHERE": {},
        "OPTIONS": {
            "COLUMNS": [
                "courses_dept",
                "courses_id",
                "courses_avg"
            ],
            "ORDER": "courses_avg"
        }
    },
    "isQueryValid": false,
    "result": "ResultTooLargeError"
}
